# 리트스 관련 메소드
list = [1,2,3]
print(list)

# 매개변수 값을 list 마지막 index에 추가
list.append(4)
print(list)

# 매개변수 A index 에 B 값을 추가
list.insert(0, 5)
print(list)

# 다량의 값을 list 마지막 index에 추가
list.extend([6,7,8])
print(list)